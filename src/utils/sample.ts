// TODO: Remove this file after first real utility is added (E0-04 cleanup)

/**
 * Sample utility function for testing infrastructure verification
 * @deprecated This is a placeholder for testing setup validation only
 */
export function greet(name: string): string {
  return `Hello, ${name}!`
}

/** @deprecated Placeholder for testing setup validation only */
export function add(a: number, b: number): number {
  return a + b
}
